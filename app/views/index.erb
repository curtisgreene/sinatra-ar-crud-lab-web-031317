<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Welcome!</title>
  </head>
  <body>
    <!-- SHOWS ALL THE POSTS -->
    <% @posts.each do |post|%>
    <h3><%= post.name %></h3>
    <p><%= post.content %></p>
    <% end %>
  </body>
</html>
